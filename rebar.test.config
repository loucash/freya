{deps, [
        {kai, ".*", {git, "https://github.com/aerosol/kai.git", {branch, "fix-leaks"}}}
]}.

{cover_enabled, true}.

{clean_files, ["test/*.beam"]}.

{ct_extra_params, "-boot start_sasl -pa ebin -pa deps/*/ebin"}.
