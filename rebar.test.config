{deps, [
        {frik, ".*", {git, "git@github.com:Ubiquiti-Data/frik.git", {branch, "develop"}}}
       ]}.

{cover_enabled, true}.

{clean_files, ["test/*.beam"]}.

{ct_extra_params, "-boot start_sasl -pa ebin -pa deps/*/ebin"}.
