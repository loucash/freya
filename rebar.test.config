{deps, [
        {frik, ".*", {git, "https://github.com/Ubiquiti-Cloud/frik.git", {branch, "develop-wip"}}}
       ]}.

{cover_enabled, true}.

{clean_files, ["test/*.beam"]}.

{ct_extra_params, "-boot start_sasl -pa ebin -pa deps/*/ebin"}.
